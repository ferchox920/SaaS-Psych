# S4_4.2 - Integridad tenant DB entre appointments y clients

## Paso
`S4_4.2`

## Objetivo
Reforzar en base de datos la integridad tenant entre `appointments` y `clients`, y validar con test de integración real en Postgres que se rechazan referencias cruzadas.

## Estado previo detectado
- Ya existía una estrategia de FK compuesta aplicada en `000007_appointments_tenant_client_fk_hardening`.
- El paso solicitado pedía nueva migración `000007_*`, pero ese número ya estaba ocupado.
- Para evitar colisión de versiones de migración, se eligió una migración incremental segura `000008_*`.

## Cambios realizados
- Se creó migración guard `000008_appointments_tenant_client_fk_guard` que:
  - asegura índice compuesto `clients (tenant_id, id)`
  - elimina FK legacy por `client_id` si existe
  - asegura (idempotente con `DO $$`) la FK compuesta `appointments(tenant_id, client_id) -> clients(tenant_id, id)`.
- Se agregó test de integración Postgres (opt-in por env):
  - crea Tenant A y Tenant B
  - crea clientes en cada tenant
  - intenta insertar appointment en Tenant A usando `client_id` de Tenant B
  - valida rechazo de Postgres con `23503 foreign_key_violation`.

## Archivos tocados
- `apps/api/migrations/000008_appointments_tenant_client_fk_guard.up.sql`
- `apps/api/migrations/000008_appointments_tenant_client_fk_guard.down.sql`
- `apps/api/internal/infra/db/appointments_tenant_fk_postgres_integration_test.go`
- `PROGRESS/PROGRESS_INDEX.md`

## Migraciones
- `000008_appointments_tenant_client_fk_guard.up.sql`
  - enforcement idempotente de FK compuesta tenant/client
- `000008_appointments_tenant_client_fk_guard.down.sql`
  - no-op (`SELECT 1`) para volver al estado previo inmediato sin revertir hardening ya consolidado

## Comandos para ejecutar
```bash
cd apps/api
go test ./...
```

```bash
# integración real Postgres
$env:RUN_PG_INTEGRATION="1"
$env:DATABASE_URL="postgres://sessionflow:sessionflow@127.0.0.1:5432/sessionflow?sslmode=disable"
go test ./internal/infra/db -run TestAppointmentsRejectCrossTenantClientReferencePostgresIntegration -v
```

## Checklist de validación
- [x] Estrategia de constraint elegida: FK compuesta tenant/client (enforcement DB).
- [x] Migración nueva agregada sin ruptura de numeración existente.
- [x] Repositorios/queries no requirieron cambios en este step.
- [x] Test de integración Postgres agregado para rechazo cross-tenant.
- [x] `go test ./...` pasa.

## Decisiones y trade-offs
- Se eligió `000008_*` en lugar de `000007_*` por seguridad operacional (evitar duplicidad de versión de migración).
- El `down` se dejó no-op para no deshacer hardening previo ya aplicado en `000007`; se priorizó no ruptura en entornos que ya tienen integridad reforzada.
- El test de rechazo cross-tenant es opt-in para no exigir Postgres en cada corrida local.

## Prompt usado
```text
Sprint: 4
Step: 4.2
Objetivo del paso: Reforzar en DB la integridad tenant entre appointments y clients. Proponer e implementar una estrategia de constraint segura: ideal FK compuesta (tenant_id, client_id) o alternativa equivalente. Crear migración nueva (000007_*). Ajustar queries/repos si es necesario. Agregar test de integración que confirme que Postgres rechaza referencias cruzadas.
Notas/Restricciones: Si FK compuesta implica refactor grande, elegir estrategia alternativa con mínima ruptura, pero documentar trade-offs.
```

## Próximo paso sugerido
`S4_5`: CI básico en GitHub Actions (`test + lint + build`) con job de servicios Postgres/Redis para integración opt-in.
